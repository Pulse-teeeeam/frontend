import TabInformation from "./components/TabInformation";

export default function pageCreatePerson({}) {
    return <div className="mt-10">
        <h1 className="font-bold text-3xl">Запись новой персоны</h1>
        <TabInformation/>
    </div>
}